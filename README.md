# LoVolt React Website

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL shown in Terminal, usually `http://localhost:5173/`.

## Build for hosting

```bash
npm run build
```

For GitHub Pages/Vercel/Netlify, upload the project and run the normal Vite build command.
